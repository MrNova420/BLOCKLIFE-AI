# BlockLife AI - npm configuration
# Copyright © 2025 WeNova Interactive / Kayden Shawn Massengill

# Note: Optional dependencies (like better-sqlite3, node-llama-cpp) are designed
# to fail gracefully on platforms without native build tools (like Termux/Android).
# The application will function normally without them.

# For Termux/Android users experiencing build failures:
# Run: npm install --omit=optional
# Or use the setup script: bash scripts/setup-termux.sh
