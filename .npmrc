# BlockLife AI - npm configuration
# Copyright © 2025 WeNova Interactive / Kayden Shawn Massengill

# Optional dependencies (bedrock-protocol, node-llama-cpp) provide:
# - Bedrock Edition Minecraft support
# - Local AI model support via llama.cpp
#
# These are not required for core functionality. The app works without them:
# - Java Edition works via mineflayer (required dependency)
# - AI works via Ollama or built-in rules
#
# If optional deps fail to compile on your platform (e.g., Termux/Android),
# the installation will continue and the app will work normally.
