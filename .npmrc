# BlockLife AI - npm configuration
# Copyright © 2025 WeNova Interactive / Kayden Shawn Massengill

# Allow installation to continue even if optional dependencies fail to install.
# This is crucial for Termux/Android environments where native modules
# like better-sqlite3 may fail to compile due to missing build tools.

# Don't fail on optional dependency installation errors
ignore-optional=false
